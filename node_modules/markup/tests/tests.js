(function() {
  var QUnit, markup, q;

  markup = require('../index');

  if (typeof QUnit === "undefined" || QUnit === null) {
    QUnit = require('qunit-cli');
  }

  q = QUnit;

  q.module('markup');

  q.test('parsePath', function() {});

}).call(this);
